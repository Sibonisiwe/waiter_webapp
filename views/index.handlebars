<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>

    <div class="row">
        <div class="heading">
            <h1>The Cuppa Cafe</h1>
        </div>
    </div>

    <hr>


    <br>

    <a href="/days">
        <div class="admin">
            <button class="awaiterAddBtn" type="submit" value="button"></button>
        </div>
    </a>


    <div class="row">
        <div class="col-12">
            {{#if messages.error}}
            <span class="flashMsg">{{messages.error}}</span>
            {{/if}}
        </div>
    </div>

    <div class="container">

        <p>Please enter your name in the URL and select working days. For example https://sibo-waiter-availability.herokuapp.com/waiter/name </p>
<div class="row">
    <div class="col-12">
        
    </div>
</div>
        <form action="/waiter/{{username}}" method="POST">

            <div class="row">
                <div class="col-12">
                    <input type="text" name="waiters" value="{{username}}" 
                        class="enterWaiterName">
                </div>
            </div>

            <div class="row">
                <div class="col-12">
                    <div class="select">
                        <strong>Select a Day</strong>
                        {{#each allDays}}
                        <input type="checkbox" name="day" class="check" value="{{this.days}}" {{this.checked}}> 
                        <span>{{this.days}}</span>
                       {{/each}}
                    </div>
                </div>
            </div>

            <div class="col-12">
                <a href=""><button class="button-primary btn" type="submit" name="button">Submit</button></a>
            </div>
        </form>


    </div>
</body>

</html>